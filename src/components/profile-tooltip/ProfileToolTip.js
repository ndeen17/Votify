import React from "react";

import "react-tooltip/dist/react-tooltip.css";
import { Tooltip } from "react-tooltip";

const ProfileToolTip = () => {
  return <Tooltip anchorId="profile-tooltip" />;
};

export default ProfileToolTip;
